<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Read connectivity to show the offline banner; INTERNET for Supabase sync. -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <!-- Parent must receive child requests (unlock/uninstall/extra time) and «protection
       broke» alerts promptly (Android 13+ runtime permission). -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <!-- allowBackup=false: этот процесс позже будет держать TOTP-секреты и токены;
       автобэкап не должен их выносить с устройства. -->
  <application
    android:name=".KiteParentApp"
    android:allowBackup="false"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/Theme.Kite">

    <activity
      android:name=".MainActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- Hands a downloaded update APK to the package installer (hms / direct builds). -->
    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.updates"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/update_paths" />
    </provider>

  </application>

</manifest>
