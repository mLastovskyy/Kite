<?xml version="1.0" encoding="utf-8"?>
<!-- Window-state changes drive foreground-app detection (M5). Content retrieval is on for
     the M6 uninstall guard: recognising the app-details / device-admin Settings screens
     needs to read the visible node text (class names alone differ too much across OEMs).
     Still scoped to no keystroke/typing capture. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
  android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
  android:accessibilityFeedbackType="feedbackGeneric"
  android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
  android:canRetrieveWindowContent="true"
  android:description="@string/accessibility_service_label"
  android:notificationTimeout="100" />
